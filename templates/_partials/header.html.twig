<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{ path('homepage') }}"><img class="logo" src="{{ asset('pictures/logo_1.png') }}" alt="logo"></a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav ms-auto mb-2 mb-lg-0">
				<li class="nav-item">
					<a class="nav-link{{ (app.request.attributes.get('_route') == 'homepage') ? ' active' }}" href="{{ path('homepage') }}">ACCUEIL</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('homepage') }}#tricks">TRICKS</a>
				</li>
				{% if app.user %}
                <li class="nav-item">
					<a class="nav-link{{ (app.request.attributes.get('_route') == 'trick_create') ? ' active' }}" href="{{ path('trick_create')}}">CREER TRICK</a>
				</li>
                <li class="nav-item">
					<a class="nav-link{{ (app.request.attributes.get('_route') == 'account_index') ? ' active' }}" href="{{ path('account_index')}}">MON COMPTE</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_logout')}}">DÉCONNEXION</a>
				</li>
				{% else %}
				<li class="nav-item">
					<a class="nav-link{{ (app.request.attributes.get('_route') == 'app_login') ? ' active' }}" href="{{ path('app_login')}}">CONNEXION</a>
				</li>
				<li class="nav-item">
					<a class="nav-link{{ (app.request.attributes.get('_route') == 'app_register') ? ' active' }}" href="{{ path('app_register')}}">INSCRIPTION</a>
				</li>
				{% endif %}
			</ul>
		</div>
	</div>
</nav>
	<!-- banner -->
	<section class="container-fluid banner mb-0">
		<div class="bannerPicture">
			<img class="ban" src="{{ asset('pictures/banner_1.jpg') }}" alt="Photo de SnowStricks">
		</div>
		<div class="inner-banner">
			<h1><img class="logo_2" src="{{ asset('pictures/logo_1.png') }}" alt="logo"></h1>
		</div>
		<!-- button -->
		{% if arrow is defined %}
		<a role="button" href="{{ path('homepage') }}#tricks" class="arrow-down"><i class="fas fa-2x fa-arrow-down"></i></a>
		{% endif %}
	</section>
	<!-- end banner -->
	{% for label, messages in  app.flashes %}
		<div class="alert alert-{{label}} alert-dismissible fade show text-center mt-0" role="alert">
		    {% for message in messages %}
		    <strong>{{message}}</strong><br>
		    {% endfor %}
		  <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
		</div>
	{% endfor %}
